{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd becraft && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "cd becraft && php artisan config:clear && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
